.DS_Store
*.backup

# Local configs with secrets — only .example templates are tracked
zsh/extra
fish/conf.d/extra.fish
git/.gitconfig.local
.claude/settings.local.json
claude/settings.local.json
article.md

# Nix netrc — contains the GitLab auth token. Set up by hand, never tracked.
nix/netrc
